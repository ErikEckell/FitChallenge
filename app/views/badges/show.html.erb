<h1><%= @badge.name %></h1>

<p><strong>Description:</strong> <%= @badge.description %></p>

<div class="mt-3">
  <% if can? :update, @badge %>
    <%= link_to "Edit", edit_badge_path(@badge), class: "btn btn-warning" %>
  <% end %>

  <% if can? :destroy, @badge %>
  <%= button_to "Delete", badge_path(badge), method: :delete,
                data: { confirm: "Are you sure?" },
                class: "btn btn-danger btn-sm" %>
  <% end %>

  <%= link_to "Back to list", badges_path, class: "btn btn-secondary" %>
</div>
